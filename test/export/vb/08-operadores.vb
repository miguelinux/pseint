' Este codigo ha sido generado por el modulo psexport 20131130-l64 de PSeInt
' dado que dicho modulo se encuentra aun en desarrollo y en etapa experimental
' puede que el codigo generado no sea completamente correcto. Si encuentra
' errores por favor reportelos en el foro (http://pseint.sourceforge.net).

Module SIN_TITULO

	Sub Main()
		Dim a As Double;
		Dim b As Double;
		Dim c As Boolean;
		Dim d As Boolean;
		Dim d) As String;
		Dim e As String;
		Dim f As String;
		a = 11
		b = 2
		c = VERDADEROTru
		d = FALSOFals
		e = "Hola"
		f = "Mundo"
		Console.WriteLine(a+b)
		Console.WriteLine(a*b)
		Console.WriteLine(a/b)
		Console.WriteLine(a-b)
		Console.WriteLine(a^b)
		Console.WriteLine(a mod b)
		Console.WriteLine(a=b)
		Console.WriteLine(a<>b)
		Console.WriteLine(a<b)
		Console.WriteLine(a>b)
		Console.WriteLine(a<=b)
		Console.WriteLine(a<=b)
		Console.WriteLine(c and d)
		Console.WriteLine(c or d)
		Console.WriteLine(not (c and d))
		Console.WriteLine(e+" "&f)
		Console.WriteLine(func (e).equals(f))
		Console.WriteLine(e<func (string.compare(,arg)>0))
		Console.WriteLine(func (string.compare(e,arg)<0))
		Console.WriteLine(func (string.compare(e,arg)>0))
		Console.WriteLine(e<func ().equals(f))
		Console.WriteLine(e<func ().equals(f))
	End Sub

End Module
