using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace PSeInt
{
    class SIN_TITULO
    {

        static void Main(string[] args)
        {

            int a;
            double b;
            bool c;
            bool d;
            string e;
            string f;

            // Se tiene que Declarar la variable p ya que si no es declarado existe un error de Contecto de la Variable 
            string p;

            a = 1;
            b = 2.5;
            c = true;
            d = false;
            e = "Hola";
            f = "Hola Mundo";
            p = Console.ReadLine();
            Console.WriteLine(p);

            // esta linea de comando es necesaria para esperar cualquier tecla para terminar el programa
            Console.ReadKey();
        }
    }
}
