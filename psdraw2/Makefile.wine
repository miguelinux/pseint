include ../mingw.conf
OBJS_DIR=../temp/drw2_w32_rls
GPP=wine ${MINGW_DIR}\\bin\\mingw32-g++.exe
CXXFLAGS= -O2 -DFREEGLUT_STATIC -I ${MINGW_DIR}\\OpenGL\\include -D_USE_TEXTURES -I ${MINGW_DIR}\\misc\\include
LIBS= -L ${MINGW_DIR}\\OpenGL\\lib -s -lfreeglut_static -lopengl32 -lglu32 -lwinmm -lgdi32 -mwindows -static-libgcc -static-libstdc++ -lwsock32 -L ${MINGW_DIR}\\misc\\lib -lpng

all: ${OBJS_DIR} ../bin/psdraw2.exe

include Makefile.common

clean:
	rm -rf ${OBJS} ../bin/psdraw2.exe

../bin/psdraw2.exe: ${OBJS}
	${GPP} ${OBJS} ${LIBS} -o $@

${OBJS_DIR}:
	mkdir ${OBJS_DIR}

